#include <iostream>
#include <string>
#include "CarteHaute.h"

#include "Carte.h"
#include "MainC.h"

using namespace std;

    //retourne 0 si le vecteur de carte correspond a une carte haute, -1 sinon
    int CarteHaute::identifier(vector<Carte> lesCartes)
    {
        int retour=0;

        if (retour==0)
        {
            return 0;
        }
        else
        {
            return -1;
        }
    }



